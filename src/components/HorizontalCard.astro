---
const { title, description, image, link } = Astro.props;
---
<div class="flex items-center bg-white shadow-lg rounded-lg overflow-hidden">
  <img class="w-1/3 h-auto" src={image} alt={title} />
  <div class="w-2/3 p-4">
    <h2 class="text-2xl font-bold">{title}</h2>
    <p class="text-gray-700 mt-2">{description}</p>
    <a href={link} class="mt-4 inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
      詳細を見る
    </a>
  </div>
</div>
